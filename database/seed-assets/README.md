# Seed Assets

Images versionnees utilisees par `database/seed.sql`.

- Place les images des menus dans `database/seed-assets/menus/`
- Lance `./scripts/sync_seed_menu_images.sh` pour les copier dans `public/images/menus/`
- Les URLs SQL doivent pointer vers `/images/menus/<fichier>`
